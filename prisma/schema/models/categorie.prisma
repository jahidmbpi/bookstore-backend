model Categorie{
  id        String     @id @default(uuid())
  name      String?
  slug      String     @unique
  image     String?
  homeImage String?
  isHomeCategory Boolean    @default(false)
  createdAt DateTime   @default(now())
  updatedAt DateTime   @updatedAt
}
  